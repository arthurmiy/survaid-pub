import 'package:flutter/material.dart';

class MajaScan {
  static Future<String> startScan({
    String title,
    Color barColor,
    Color titleColor,
    Color qRCornerColor,
    Color qRScannerColor,
    bool flashlightEnable,
    double scanAreaScale,
  }) async {
    return '';
  }
}
